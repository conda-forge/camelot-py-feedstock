{% set name = "camelot-py" %}
{% set version = "0.3.2" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

source:
  url: https://github.com/socialcopsdev/camelot/archive/v{{ version }}.tar.gz
  sha256: 02aebf2b8e86c8f962f5c21f851c164d75ddbe1c9d1d01f2c4987c1d2dcfdd1a

build:
  number: 0
  entry_points:
    - camelot = camelot.cli:cli
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  host:
    - python
    - setuptools
  run:
    - tk
    - ghostscript  # [not win]
    - click >=6.7
    - matplotlib >=2.2.3
    - numpy >=1.13.3
    - opencv >=3.2.0
    - openpyxl >=2.5.8
    - pandas >=0.23.4
    - pdfminer.six >=20170720
    - pycryptodome
    - pypdf2 >=1.26.0
    - python

test:
  imports:
    - camelot
  commands:
    - camelot --help

about:
  home: http://camelot-py.readthedocs.io/
  license: MIT
  license_file: LICENSE
  summary: PDF Table Extraction for Humans.

extra:
  recipe-maintainers:
    - vinayak-mehta
